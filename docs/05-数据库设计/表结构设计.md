## NESOM 表结构设计（MVP）

版本: 1.1  
日期: 2026-04-10  
作者: AI Coding Agent（基于实际代码更新）  
状态: Draft  
审核状态: 待评审

## 0. 说明

- 数据库：MySQL 8.0（推荐 8.0.30+）
- 字符集：`utf8mb4`，排序规则：`utf8mb4_unicode_ci`
- 本文件给出 **MVP 的数据库基线表结构**，以 `docs/06-详细设计/*/数据库设计.md` 中的“模块数据库详细设计”为准绳进行统一与收敛。
- 由于高频时序数据/审计日志等表对性能要求更高，主键与时间字段精度会与普通业务表不同（见各模块说明）。

> 如需直接落地 SQL 脚本，建议在后续新增 `docs/05-数据库设计/init.sql` 或迁移工具（如 Alembic）并与本表结构保持一致。

### 0.1 主键与ID策略（对齐现有详细设计）

目前 `docs/06-详细设计/*/数据库设计.md` 呈现出两类主键策略并存：

- **业务主表（多数）**：`id VARCHAR(36)`，默认 `UUID()`（如设备、用户、角色、工单等）
- **高频明细/日志（部分）**：`id BIGINT AUTO_INCREMENT`（如指标采集、审计/日志类、库存明细等）

本文件将采用与详细设计一致的“混合策略”，并在每个表段落中标注。

### 0.2 重要说明：本文只做“数据库基线”

- `docs/06-详细设计/*/数据库设计.md` 会包含更完整的字段、容量规划、分区、缓存与安全设计；本文件不重复所有字段，重点保证**表清单、关键字段、关键索引/约束、枚举口径**统一。
- 任何新增表/字段应先更新对应模块的 `数据库设计.md`，再回填到这里的基线中。

## 1) 设备监控模块

对应详细设计：`docs/06-详细设计/01-设备监控模块/数据库设计.md`

### 1.1 `device_types`（新增，设备分类树）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE device_types (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  type_code VARCHAR(50) NOT NULL,
  type_name VARCHAR(100) NOT NULL,
  parent_id VARCHAR(36),
  category VARCHAR(50),
  parameter_template JSON,
  maintenance_template JSON,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_type_code (type_code),
  INDEX idx_category (category),
  INDEX idx_parent_id (parent_id),
  FOREIGN KEY (parent_id) REFERENCES device_types(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 1.2 `devices`（更新：对齐 UUID + 场站/类型关联）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE devices (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  station_id VARCHAR(36) NOT NULL,
  device_type_id VARCHAR(36) NOT NULL,
  device_code VARCHAR(50) NOT NULL,
  device_name VARCHAR(100) NOT NULL,
  status VARCHAR(20) NOT NULL DEFAULT 'offline',
  manufacturer VARCHAR(100),
  model VARCHAR(100),
  parameters JSON,
  last_heartbeat DATETIME,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_station_device_code (station_id, device_code),
  INDEX idx_status (status),
  INDEX idx_device_type (device_type_id),
  INDEX idx_station (station_id),
  INDEX idx_station_status (station_id, status),
  INDEX idx_manufacturer (manufacturer),
  FOREIGN KEY (device_type_id) REFERENCES device_types(id) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

> 注：`stations` 表在现有文档中被多处引用（设备、工单、巡检等），但尚未在本仓库形成统一的“公共主数据”基线表定义；建议在后续数据库评审中补齐“公共主数据（场站/区域/供应商/租户等）”章节。

### 1.3 `device_metrics`（更新：对齐 UUID 外键 + 毫秒精度）

主键：`BIGINT`（自增）；外键：`device_id VARCHAR(36)`  
建议：按月分区（详见模块详细设计）

```sql
CREATE TABLE device_metrics (
  id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  device_id VARCHAR(36) NOT NULL,
  metric_type VARCHAR(50) NOT NULL,
  metric_value DECIMAL(12, 4) NOT NULL,
  metric_unit VARCHAR(20) NOT NULL,
  quality TINYINT DEFAULT 100,
  collected_at DATETIME(3) NOT NULL,
  received_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  source VARCHAR(50) DEFAULT 'direct',
  tags JSON,
  INDEX idx_device_collected (device_id, collected_at DESC),
  INDEX idx_collected_at (collected_at),
  INDEX idx_metric_type (metric_type),
  FOREIGN KEY (device_id) REFERENCES devices(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 1.4 `device_alerts`（更新：补充 alert_code/标题/状态机）

主键：`BIGINT`（自增）；外键：`device_id VARCHAR(36)`

```sql
CREATE TABLE device_alerts (
  id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  device_id VARCHAR(36) NOT NULL,
  alert_code VARCHAR(50) NOT NULL,
  alert_type VARCHAR(50) NOT NULL,
  alert_level ENUM('info','warning','error','critical') NOT NULL,
  alert_title VARCHAR(200) NOT NULL,
  alert_message TEXT NOT NULL,
  alert_data JSON,
  start_time DATETIME(3) NOT NULL,
  end_time DATETIME(3),
  acknowledged_at DATETIME,
  acknowledged_by VARCHAR(36),
  resolved_at DATETIME,
  resolved_by VARCHAR(36),
  resolution_notes TEXT,
  status ENUM('active','acknowledged','resolved','closed') NOT NULL DEFAULT 'active',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_alert_code (alert_code),
  INDEX idx_device_status (device_id, status),
  INDEX idx_alert_level (alert_level),
  INDEX idx_start_time (start_time DESC),
  FOREIGN KEY (device_id) REFERENCES devices(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 1.5 设备阈值/事件/维护（新增：基线表清单）

详细字段与分区/保留策略见设备模块数据库设计文档。建议在 MVP 基线中至少包含：

- `device_thresholds`：设备/类型/全局阈值配置（支持生效时间、表达式）
- `device_events`：设备事件（启停、模式、参数变更等，审计与追溯）
- `device_maintenance_logs`：设备维护记录（与工单/备件可关联）

## 2) 工单管理模块

对应详细设计：`docs/06-详细设计/02-工单管理模块/数据库设计.md`

### 2.1 `work_orders`（更新：对齐 UUID + 字段命名）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE work_orders (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  work_order_no VARCHAR(50) NOT NULL,
  work_order_type ENUM('repair','inspection','maintenance','fault','other') NOT NULL DEFAULT 'repair',
  title VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  status ENUM('draft','pending','assigned','in_progress','pending_review','completed','closed','cancelled') NOT NULL DEFAULT 'draft',
  priority ENUM('low','medium','high','emergency') NOT NULL DEFAULT 'medium',
  emergency_level ENUM('normal','urgent','critical') NOT NULL DEFAULT 'normal',
  station_id VARCHAR(36) NOT NULL,
  device_id VARCHAR(36),
  reported_by VARCHAR(36) NOT NULL,
  reported_by_name VARCHAR(100) NOT NULL,
  reported_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  assigned_to VARCHAR(36),
  assigned_to_name VARCHAR(100),
  assigned_at DATETIME,
  scheduled_start DATETIME,
  scheduled_end DATETIME,
  actual_start DATETIME,
  actual_end DATETIME,
  completion_rate INT DEFAULT 0,
  attachments JSON,
  tags JSON,
  closed_at DATETIME,
  archived_at DATETIME,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_work_order_no (work_order_no),
  INDEX idx_status (status),
  INDEX idx_priority (priority),
  INDEX idx_type (work_order_type),
  INDEX idx_station_status (station_id, status),
  INDEX idx_assigned_status (assigned_to, status),
  INDEX idx_reported_at (reported_at DESC),
  FOREIGN KEY (device_id) REFERENCES devices(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 2.2 工单扩展表（新增：基线表清单）

详细字段见工单模块数据库设计文档。建议在 MVP 基线中至少包含：

- `work_order_details`：处理步骤明细（`BIGINT` 自增）
- `work_order_status_history`：状态变更历史（`BIGINT` 自增）
- `work_order_evaluations`：评价与反馈（`BIGINT` 自增）
- `work_order_templates`：模板（`VARCHAR(36)`）
- `work_order_spare_parts`：工单-备件关联（`BIGINT` 自增）

> 说明：早期概要设计中的 `work_order_logs` 在详细设计中被“步骤明细 + 状态历史 + 附件/图片字段”更细化替代；是否保留 `work_order_logs` 作为统一日志表，建议在评审中决策。

## 3) 用户权限（RBAC）

对应详细设计：`docs/06-详细设计/03-用户权限管理/数据库设计.md`

### 3.1 `users`（更新：对齐 UUID + 安全字段 + 软删除）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE users (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  username VARCHAR(50) NOT NULL,
  email VARCHAR(100) NOT NULL,
  phone VARCHAR(20),
  password_hash VARCHAR(255) NOT NULL,
  real_name VARCHAR(50) NOT NULL,
  department_id VARCHAR(36),
  position VARCHAR(50),
  status ENUM('active','inactive','locked','pending') NOT NULL DEFAULT 'active',
  failed_login_attempts INT NOT NULL DEFAULT 0,
  lockout_until DATETIME,
  mfa_enabled BOOLEAN NOT NULL DEFAULT FALSE,
  last_login_at DATETIME,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at DATETIME,
  UNIQUE KEY uk_username (username),
  UNIQUE KEY uk_email (email),
  UNIQUE KEY uk_phone (phone),
  INDEX idx_status (status),
  INDEX idx_department (department_id),
  INDEX idx_username_status (username, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.2 `roles`（更新：对齐 UUID + 角色类型/数据范围）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE roles (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  role_code VARCHAR(50) NOT NULL,
  role_name VARCHAR(100) NOT NULL,
  description TEXT,
  role_type ENUM('system','custom','department') NOT NULL DEFAULT 'custom',
  data_scope ENUM('all','department','self','custom') NOT NULL DEFAULT 'self',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_role_code (role_code),
  INDEX idx_role_type (role_type),
  INDEX idx_is_default (role_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.3 `permissions`（更新：对齐 module/resource/action）

主键：`VARCHAR(36)`（UUID）

```sql
CREATE TABLE permissions (
  id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
  perm_code VARCHAR(100) NOT NULL,
  perm_name VARCHAR(100) NOT NULL,
  module VARCHAR(50) NOT NULL,
  resource VARCHAR(50) NOT NULL,
  action ENUM('create','read','update','delete','execute','manage') NOT NULL DEFAULT 'read',
  description TEXT,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_perm_code (perm_code),
  INDEX idx_module_resource_action (module, resource, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.4 `user_roles`（更新：对齐自增id + 过期/生效）

主键：`BIGINT`（自增）

```sql
CREATE TABLE user_roles (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id VARCHAR(36) NOT NULL,
  role_id VARCHAR(36) NOT NULL,
  assigned_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  expires_at DATETIME,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  UNIQUE KEY uk_user_role (user_id, role_id),
  INDEX idx_user_id (user_id),
  INDEX idx_role_id (role_id),
  INDEX idx_expires_at (expires_at),
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.5 `role_permissions`（更新：对齐自增id + deny/条件）

主键：`BIGINT`（自增）

```sql
CREATE TABLE role_permissions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_id VARCHAR(36) NOT NULL,
  permission_id VARCHAR(36) NOT NULL,
  granted_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  is_deny BOOLEAN NOT NULL DEFAULT FALSE,
  condition JSON,
  UNIQUE KEY uk_role_permission (role_id, permission_id),
  INDEX idx_role_id (role_id),
  INDEX idx_permission_id (permission_id),
  FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE,
  FOREIGN KEY (permission_id) REFERENCES permissions(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.6 权限扩展表（新增：基线表清单）

- `departments`：组织树（UUID）
- `user_sessions`：会话（UUID）
- `audit_logs`：审计日志（`BIGINT` 自增，建议分区，保留 180 天）
- `login_attempts`：登录尝试（`BIGINT` 自增，建议分区）

## 4) 备件管理模块

对应详细设计：`docs/06-详细设计/04-备件管理模块/数据库设计.md`

备件模块在详细设计中比概要设计扩展显著，建议 MVP 基线至少包含以下实体：

- `spare_part_categories`（UUID，树）
- `spare_parts`（UUID，包含库存汇总字段）
- `warehouses`（UUID）
- `storage_locations`（UUID）
- `inventory_details`（`BIGINT` 自增，库存事务明细）
- `inventory_snapshots`（`BIGINT` 自增，含乐观锁 version）
- `purchase_orders` / `purchase_order_items`
- `transfer_orders`
- `inventory_orders`

> 由于表数量与字段较多，本文件不展开所有字段；落地时以备件模块详细设计为准，并将其 SQL 抽取到 `init.sql`/迁移脚本。

## 5) 巡检管理模块

对应详细设计：`docs/06-详细设计/05-巡检管理模块/数据库设计.md`

巡检模块在详细设计中扩展为“计划/路线/检查点/任务/结果/模板/分类/排班”等，建议 MVP 基线至少包含：

- `inspection_plans`（UUID）
- `inspection_routes`（UUID）
- `inspection_checkpoints`（UUID）
- `inspection_tasks`（UUID，含离线同步字段）
- `inspection_results`（UUID，移动端数据：照片/视频/签名/GPS）
- `inspection_templates`（UUID）
- `inspection_categories`（UUID，树）
- `inspection_schedules`（UUID）

## 6) 报表统计模块

对应详细设计：`docs/06-详细设计/06-报表统计模块/数据库设计.md`

报表模块在详细设计中以“模板/计划/执行/收藏/数据源/缓存/仪表盘配置”为核心，建议 MVP 基线至少包含：

- `report_templates`（UUID）
- `report_schedules`（UUID）
- `report_executions`（UUID + `execution_id` 业务唯一）
- `report_favorites`（UUID）
- `data_sources`（UUID）
- `statistics_cache`（UUID）
- `dashboard_configs`（UUID）
- 预聚合/预计算表：`daily_statistics_agg`、`monthly_statistics_agg`、`device_efficiency_precalc`（按需求逐步落地）

## 7) 系统配置模块

对应详细设计：`docs/06-详细设计/07-系统配置模块/数据库设计.md`

系统配置模块当前详细设计与其他模块存在“ID策略/字段命名”差异（它使用 `BIGINT` 自增 + `tenant_id` 多租户字段）。建议在数据库评审中明确：

- 是否全系统统一多租户（`tenant_id`）字段
- 是否统一 `created_at/updated_at` 命名（该文档使用 `created_time/updated_time`）
- 是否将 `sys_audit_log` 与权限模块的 `audit_logs` 合并或分层

基于现有详细设计，建议 MVP 基线至少包含：

- `sys_config`：系统参数（`BIGINT` 自增，含 `tenant_id`，唯一键 `(tenant_id, config_key)`）
- `sys_dict`：字典（`BIGINT` 自增，含 `tenant_id`，树结构）
- `sys_approval_flow`：审批流程配置
- `sys_notice_template`：通知模板
- `sys_log_config`：日志配置
- `sys_audit_log`：审计日志（与权限模块审计表关系需评审统一）

## 8) 集成与运维专用数据库（新增：基线表清单）

对应详细设计：

- 集成：`docs/06-详细设计/08-系统集成设计/数据库设计.md`
- 运维：`docs/06-详细设计/09-部署运维设计/数据库设计.md`

这些表更偏“平台能力”，可独立库（如 `integration_db` / `operations_db`）：

- **集成**：`integration_events`（建议分区）、`data_sync_records`、`api_call_logs`、`mq_status_monitor`、`data_mapping_rules`、`integration_health_status`
- **运维**：`environments`、`deployment_configs`、`deployment_history`（分区）、`monitoring_configs`、`alert_records`（分区）、`backup_policies`、`backup_records`（分区）、`recovery_records`、`operation_tickets`、`capacity_planning`、`performance_metrics`（分区）等


